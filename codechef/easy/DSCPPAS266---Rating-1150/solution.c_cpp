bool canReduce(int N, vector<int>& arr) {
    //write your code here
    sort(arr.begin(),arr.end());
    int flag=1;
    for(int i=0;i<N-1;i++){
        if(abs(arr[i]-arr[i+1])>1){
            flag=0;
        }
    
    }if(flag) return true;
    else return false;
}