#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        int re1,re2,re0;
        vector<int> a(n);
        for(int i=0;i<n;i++){
            cin>>a[i];
            if(a[i]%3==0) re0++;
            else if(a[i]%3==1) re1++;
            else re2++;
        }
        if(re0>=1||re1>=3||re>=3 || (re1>=1 && re2>=1)) cout<<"yes"<<endl;
        else cout<<"no"<<endl;
    }
}
