class Solution {
public:
    int countNonMinimum(vector<int>& nums) {
        // write your code here 
        sort(nums.begin(),nums.end());
        int c=count(nums.begin(),nums.end(),nums[0]);
        return nums.size()-c;
    }
};
